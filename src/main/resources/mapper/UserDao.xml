<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootdemo.dao.UserDao">

    <resultMap id="UserResultMap" type="UserInfo">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phoneNum" property="phoneNum"/>
        <result column="createDate" property="createDate"/>
        <collection property="roles" resultMap="RoleResultMap"/>
    </resultMap>
    <resultMap id="RoleResultMap" type="Role">
        <id column="roleId" property="id"/>
        <result column="roleName" property="roleName"/>
        <collection property="privileges" resultMap="PrivilegeResultMap"/>
    </resultMap>
    <resultMap id="PrivilegeResultMap" type="Privilege">
        <result column="privilegeId" property="id"/>
        <result column="privilegeName" property="privilegeName"/>
    </resultMap>

    <select id="findUserByUsername" resultMap="UserResultMap">
        SELECT
          u.*,
          r.id AS roleId,
          r.role_name AS roleName,
          p.id AS privilegeId,
          p.privilege_name AS privilegeName
        FROM
          demo_user u
          LEFT JOIN demo_user_role ur ON u.id = ur.user_id
          LEFT JOIN demo_role r ON ur.role_id = r.id
          LEFT JOIN demo_role_privilege rp ON r.id = rp.role_id
          LEFT JOIN demo_privilege p ON rp.privilege_id = p.id
        WHERE
          u.username = #{username}
    </select>

</mapper>